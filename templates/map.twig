{% extends "base.twig" %}

{% block title %}{{ title }}{% endblock %}
{% block content %}
    <div class="w-section section">
        <div class="map">
            <a target='_blank' href='{{ anchor_href }}'>
                <img src='{{ img_href }}'>
            </a>
        </div>
        <div class="locDesc">{{ location_name }}<br>{{ location_description | raw }}</div>
    </div>
{% endblock %}
